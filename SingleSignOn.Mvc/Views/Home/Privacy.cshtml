@{
    ViewData["Title"] = "Privacy Policy";
}
<h1>@ViewData["Title"]</h1>

<p>Use this page to detail your site's privacy policy.</p>

@if (User.Identity.IsAuthenticated)
{
    <h3>@User.Claims.FirstOrDefault(c => c.Type == "name")?.Value</h3>
}
